// import type { RequestHandler } from "express";

// import artistPhotoRepository from "./artistPhotoRepository";

// const browse: RequestHandler = async (req, res, next) => {
//   try {
//     const photos = await artistPhotoRepository.readAll(artist_id);

//     res.json(photos);
//   } catch (err) {
//     next(err);
//   }
// };
